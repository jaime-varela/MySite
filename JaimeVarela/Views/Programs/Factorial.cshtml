@{
    if(@ViewData["result"] == null)
    {
        ViewData["result"] = "Results shown here.";
    }
    if(@ViewData["input"] == null)
    {
        ViewData["input"] = "";
    }
}

<br><br>

<p>The following form uses ASP.NET's built in big integer library to compute factorials.  Below a certain value (about 500) the factorial 
    is calculated via standard recusion, above that value the factorial is calculated via split-recursive algorithm.
</p>

<br><br>

<form accept-charset="0123456789" asp-action="Factorial" asp-controller="Programs">
    <div class="form-group">
                <label name="integer">Please Enter an Integer</label>
                <input name="integer" type="text" class="form-control" value=@ViewData["input"]>
    </div>
    <div class="form-group">
                <input type="submit" value="result" class="btn btn-default">
    </div>
</form>

<div>
    <p align="center" style="word-wrap: break-word;">@ViewData["result"]</p>
</div>
